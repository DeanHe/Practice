package main

func generate(numRows int) [][]int {
	res := make([][]int, numRows)
	for r := 0; r < numRows; r++ {
		res[r] = make([]int, r+1)
		res[r][0] = 1
		res[r][r] = 1
		for c := 1; c < r; c++ {
			res[r][c] = res[r-1][c-1] + res[r-1][c]
		}
	}
	return res
}
